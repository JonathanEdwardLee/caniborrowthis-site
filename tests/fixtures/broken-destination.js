/** TEST-ONLY broken destination fixture — excluded from production source data. */
export const BROKEN_DESTINATION_FIXTURE = {
  id: 'TEST_BROKEN_DESTINATION',
  testOnly: true,
  url: 'https://invalid.test.example/broken',
  class: 'RELEVANT_BORROWING_PROGRAM',
};
