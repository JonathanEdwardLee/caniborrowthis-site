{
  "name": "caniborrowthis-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npm run test:unit && npm run test:browser",
    "test:unit": "node --test tests/**/*.test.js",
    "test:browser": "node --test tests/browser.test.mjs",
    "generate:ozarks": "node scripts/generate-ozarks-data.mjs",
    "generate:national": "node scripts/generate-national-data.mjs",
    "capture-evidence": "node tests/capture-evidence.mjs",
    "postinstall": "playwright install chromium",
    "serve": "python3 -m http.server 8080"
  },
  "devDependencies": {
    "playwright": "1.62.1"
  }
}
